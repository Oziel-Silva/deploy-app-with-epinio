name: Hello World

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: arc-runner-k8s
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print Hello World
        run: echo "Hello, World!!"
